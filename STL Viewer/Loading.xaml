<Window x:Class="StlViewer.Loading"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StlViewer"
        mc:Ignorable="d"
        Title="{DynamicResource Loading_Title}" Height="150" Width="318" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" UseLayoutRounding="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="text" Margin="0,10,0,0" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Top" Padding="3,0" TextTrimming="WordEllipsis" Height="35"/>
        <ProgressBar x:Name="progressbar" Height="15" Margin="15,0,15,25" VerticalAlignment="Bottom" Maximum="1" LargeChange="0.1" SmallChange="0.01"/>
        <Button Content="{DynamicResource Loading_cancel}" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center" Width="75" Grid.Row="1" IsDefault="True" IsCancel="True"/>
    </Grid>
</Window>
